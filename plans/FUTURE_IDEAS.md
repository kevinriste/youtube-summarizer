# Future Ideas

- Persist `activeResponseId` in `localStorage` so follow-up Q&A can continue after a page refresh.
  - Notes: Clear it when a new transcript summary starts to avoid accidentally chaining follow-ups into the wrong conversation.
