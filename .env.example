# OpenAI API key (required)
OPENAI_API_KEY=sk-...

# Model to use (e.g., gpt-4o-mini, gpt-4o, gpt-3.5-turbo)
OPENAI_MODEL=gpt-4o-mini

# Token limits (adjust according to your subscription/model constraints)
OPENAI_MAX_TOTAL_TOKENS=127000
OPENAI_MAX_RESPONSE_TOKENS=4096

# Password required to call the summary API (set a strong value)
API_PASSWORD=change-me
